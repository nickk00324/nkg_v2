import styled from "styled-components";

// so this just basically makes it so when there is just a titlecard only (ex. loading)
// it wont collapse the page and make the ugly flash

const KeepHeight = styled.div`
  height: 100vh;
`;

export default KeepHeight;
